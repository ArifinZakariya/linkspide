{
  "name": "shortlink-bypass",
  "version": "1.0.0",
  "description": "Modular web tool to bypass multi-layer ad links and shortlinks",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "postinstall": "npx puppeteer browsers install chrome",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12"
  },
  "optionalDependencies": {
    "puppeteer": "^22.0.0"
  }
}
